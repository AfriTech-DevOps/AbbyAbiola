apiVersion: apps/v1
kind: Deployment
metadata:
  name: rapheebeauty-dev
  namespace: dev-namespace
  labels:
    app: rapheebeauty-dev
spec:
  replicas: 2
  selector:
    matchLabels:
      app: rapheebeauty-dev
  template:
    metadata:
      labels:
        app: rapheebeauty-dev
    spec:
      containers:
        - name: rapheebeauty-dev-latest
          image: abimbola1981/rapheebeauty-dev:latest
          ports:
            - containerPort: 8000
          resources:
            limits:
              memory: "1000Mi"
              cpu: "500m"
            requests:
              memory: "500Mi"
              cpu: "250m"
          env:
            - name: MYSQL_HOST
              valueFrom:
                configMapKeyRef:
                  name: rapheebeauty-db-config
                  key: MYSQL_HOST
            - name: MYSQL_USER
              valueFrom:
                configMapKeyRef:
                  name: rapheebeauty-db-config
                  key: MYSQL_USER
            - name: MYSQL_PASSWORD
              valueFrom:
                configMapKeyRef:
                  name: rapheebeauty-db-config
                  key: MYSQL_PASSWORD
            - name: MYSQL_DATABASE
              valueFrom:
                configMapKeyRef:
                  name: rapheebeauty-db-config
                  key: MYSQL_DATABASE

